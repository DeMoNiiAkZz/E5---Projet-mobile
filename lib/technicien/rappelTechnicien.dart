import 'package:flutter/material.dart';

class RappelTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Rappel', style: TextStyle(fontSize: 24)),
    );
  }
}